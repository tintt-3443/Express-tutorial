extends ../layout

block content
  h1 #{t('home.list-genres')}
  if genres.length
    ul
      each genre in genres
        li
          a(href=genre.url()) #{genre.name}
  else
    p #{t('home.genre-empty')}
